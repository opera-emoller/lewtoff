[package]
name = "lewtoff-cli"
version = "0.0.0"
edition = "2024"
publish = false

[[bin]]
name = "lewtoff"
path = "src/main.rs"

[features]
parallel = ["lewtoff/parallel"]

[dependencies]
lewtoff = { path = "../.." }
